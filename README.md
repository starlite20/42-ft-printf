# 42-ft-printf
Reimplementation of the C printf() function supporting multiple format specifiers, flags, width, and precision.

Learnings
- Explored the Printf about building the basic functionality with foramt specifiers
- Recognized differnece in outputs between Mac System and Linux system, where pointers of null are represented differently in the original function.
- Iterativily built the format specifier flags support and enhanced the code in detail.